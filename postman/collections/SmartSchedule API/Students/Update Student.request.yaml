$kind: http-request
name: Update Student
method: PATCH
url: '{{base_url}}/students/{{student_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Nguyen Van A Updated"
    }
description: 'Update a student record by student_id.'
order: 3000
