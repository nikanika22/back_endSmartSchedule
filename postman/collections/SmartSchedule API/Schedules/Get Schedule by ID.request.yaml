$kind: http-request
name: Get Schedule by ID
method: GET
url: '{{base_url}}/schedules/{{schedule_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Get a single schedule by ID. JWT required.'
order: 3000
