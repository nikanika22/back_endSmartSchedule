$kind: http-request
name: Delete Schedule
method: DELETE
url: '{{base_url}}/schedules/{{schedule_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Delete a schedule by ID. JWT required.'
order: 5000
