$kind: http-request
name: Get Course by ID
method: GET
url: '{{base_url}}/courses/{{course_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Get a single course by course_id (string).'
order: 3000
