$kind: http-request
name: Get Classes for Course
method: GET
url: '{{base_url}}/courses/{{course_id}}/classes'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Get all class sections belonging to a specific course.'
order: 4000
