$kind: http-request
name: Get Personal Event by ID
method: GET
url: '{{base_url}}/personal-events/{{event_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Get a single personal event by ID. JWT required.'
order: 3000
