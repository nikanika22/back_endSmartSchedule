$kind: http-request
name: Get Class by ID
method: GET
url: '{{base_url}}/classes/{{class_id}}'
auth:
  type: bearer
  credentials:
    token: '{{access_token}}'
description: 'Get a single class section by class_id (string).'
order: 3000
